def rangee(numbers):

    numbers = [1, 10, 33, 21, 2, 6, 3]

    #minimum = numbers[0]
    maximum = numbers[0]

    for number in numbers(numbers):
        if numbers > maximum:
            maximum = numbers
        result = maximum    
    return result

