const prompt = require("prompt-sync") ();
let firstNumber = prompt("Enter First Number: ");
let secondNumber = prompt("Enter Second Number: ");

console.log("Your first Input is:",firstNumber, " and second Input is:",secondNumber);
